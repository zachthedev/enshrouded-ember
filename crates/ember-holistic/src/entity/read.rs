//! Reading a component off an entity.
//!
//! `World` lives here: the borrow every accessor in Ember takes, carrying the
//! addresses the host resolved. Nothing in this crate resolves a symbol itself.
