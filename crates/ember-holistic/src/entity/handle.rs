//! `Entity`, the handle the engine identifies an entity by.
//!
//! It is a bare `u32`, matching the engine's own four-byte entity id, and it is
//! transparent so it can sit unchanged inside a component or a hook argument.
