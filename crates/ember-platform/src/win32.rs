//! Declarations for the Win32 entry points Ember calls.
//!
//! Roughly twenty classic exports, each one a `windows_link::link!` line. The
//! set is small enough to read, so the crate carries no binding generator and no
//! metadata file.
