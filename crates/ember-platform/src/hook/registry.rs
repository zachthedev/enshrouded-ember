//! The hook registry.
//!
//! One inline patch per target, with a chain of links above it in safe Rust.
//! The registry keys on the resolved address, because the linker can fold two
//! symbols onto one function.
