//! The events that drive the client's banner and feedback widgets.
//!
//! Each carries a localization tag rather than text, and the dedicated server
//! ships no localization table, so a caller carries every id it displays.
