//! `SymbolId`, the name a signature row resolves.
//!
//! A symbol id is Ember's own name for a function. It is never a name the
//! binary registers for itself, because a registered name reaches a data record
//! and never an instruction.
